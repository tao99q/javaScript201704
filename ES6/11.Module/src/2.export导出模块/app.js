import E, {cnt, func, Util, x, a1, a2, a3, y, obj1, obj2, p, q, q1, q2, z, z1, z2} from './Export';

console.log(E);

console.log(cnt, func, Util, x, a1, a2, a3, y, obj1, obj2, p, q, q1, q2, z, z1, z2);

//q = 123;
console.log(q, '=======-==--=-==-');
// console.log(q, q1, q2, '--');
setTimeout(() => {
  console.log(q);
  // console.log(q, q1, q2, '===');
}, 1000);

// cnt = 11; // 写了这句话会编译不通过